export const z = {
  string: () => ({
    safeParse(value) {
      return typeof value === 'string' ? { success: true, data: value } : { success: false };
    }
  }),
  object: () => ({
    safeParse(value) {
      return { success: true, data: value };
    }
  })
};
export default { z };
